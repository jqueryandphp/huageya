import { Handler } from './index';
/**
 * render html handlers pipe
 * const resultHtml = compose(
 *   handler1,
 *   handler2,
 *   hanlder3,
 *   ...
 * )($, args);
 *
 * @param handler ($, args) => $
 */
declare const compose: (...handler: Handler[]) => Handler;
export default compose;
